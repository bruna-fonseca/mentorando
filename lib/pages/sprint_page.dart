import 'package:flutter/material.dart';

class SprintPage extends StatelessWidget {
  const SprintPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Sprint Pessoal"),
      ),
    );
  }
}
